# R.C.T.F.

- **Role:**
Ты — Senior QA Performance Engineer. Отвечаешь за формирование проверяемых нагрузочных сценариев и пределов для сервиса ревью PR.
- **Context:**
- Репозиторий practices/practice_01/ с заполненными файлами: adr.md, analysis.md, problem.md, prompts.md, tests_*.
- Ключевые факты:
  - Планируется явная валидация входа и ограничение размера diff (ADR).
  - LLM-клиент должен работать с таймаутом ~10s и контролируемыми ошибками (ADR).
  - Риски подтверждены в prompts.md: 500 при отсутствии diff, 500 при исключении LLM, деградация/ошибка при большом diff.
- Цель: снизить 5xx, ввести явный контракт (problem.md), сохранять проверяемость (prompts.md).
- **Task:**
1) Перепроектируй practices/practice_01/tests_load.md:
   - Сформируй 4–5 сценариев с измеримыми «Нагрузка и длительность», «Допустимый предел», «Что измеряем».
   - Для каждого сценария приведи Evidence в формате file:line из репозитория (например, app/api.py:35–38; adr.md; analysis.md; prompts.md с ID).
   - Числа по RPS/латентности, если вводишь, пометь как «Гипотеза» и предложи шаги замера (инструмент, длительность, метрика P95).
2) Явно разреши коды 413/422 для граничных случаев, запрети 500.
3) В конце верни краткий блок «Как использовали AI» с:
   - Строка в prompts.md: P2-RCTF
   - Что проверили и исправили сами: что именно переписано/уточнено.
- **Format:**
- Открой practices/practice_02/rctf/experiment.md и заполни:
  - Role/Context/Task/Format из этого промпта (перенеси текст в соответствующие поля)
  - В «Полный запрос» вставь весь этот промпт
  - В «Что получили» помести готовые разделы для tests_load.md: таблицу и блок «Как использовали AI»
  - В «Что изменили в исходном артефакте» укажи конкретику правок в practices/practice_01/tests_load.md и как это проверено ссылками на adr.md/analysis.md/problem.md/prompts.md

## Полный запрос

Role:
Ты — Senior QA Performance Engineer. Отвечаешь за формирование проверяемых нагрузочных сценариев и пределов для сервиса ревью PR.

Context:
- Репозиторий practices/practice_01/ с заполненными файлами: adr.md, analysis.md, problem.md, prompts.md, tests_*.
- Ключевые факты:
  - Планируется явная валидация входа и ограничение размера diff (ADR).
  - LLM-клиент должен работать с таймаутом ~10s и контролируемыми ошибками (ADR).
  - Риски подтверждены в prompts.md: 500 при отсутствии diff, 500 при исключении LLM, деградация/ошибка при большом diff.
- Цель: снизить 5xx, ввести явный контракт (problem.md), сохранять проверяемость (prompts.md).

Task:
1) Перепроектируй practices/practice_01/tests_load.md:
   - Сформируй 4–5 сценариев с измеримыми «Нагрузка и длительность», «Допустимый предел», «Что измеряем».
   - Для каждого сценария приведи Evidence в формате file:line из репозитория (например, app/api.py:35–38; adr.md; analysis.md; prompts.md с ID).
   - Числа по RPS/латентности, если вводишь, пометь как «Гипотеза» и предложи шаги замера (инструмент, длительность, метрика P95).
2) Явно разреши коды 413/422 для граничных случаев, запрети 500.
3) В конце верни краткий блок «Как использовали AI» с:
   - Строка в prompts.md: P2-RCTF
   - Что проверили и исправили сами: что именно переписано/уточнено.

Куда записать результат:
- Открой practices/practice_02/rctf/experiment.md и заполни:
  - Role/Context/Task/Format из этого промпта (перенеси текст в соответствующие поля)
  - В «Полный запрос» вставь весь этот промпт
  - В «Что получили» помести готовые разделы для tests_load.md: таблицу и блок «Как использовали AI»
  - В «Что изменили в исходном артефакте» укажи конкретику правок в practices/practice_01/tests_load.md и как это проверено ссылками на adr.md/analysis.md/problem.md/prompts.md

## Что получили

Готовые разделы для tests_load.md:

Политика кодов ошибок (для всех сценариев):
- Разрешены: 413 (слишком большой diff), 422 (невалидный вход).
- Запрещён: 500 (generic). Ошибки LLM маппятся в контролируемый ответ без 500, согласно ADR.

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Большой diff (граничный размер) | 1 запрос с diff длиной N символов. N — гипотеза; подобрать бинарным поиском до срабатывания лимита. Инструмент: k6/vegeta; длительность шага: до получения ответа. | 413 или 422; без 500. Латентность p95 ≤ 800ms (Гипотеза) на отказ с ранней валидацией. | Код ответа; p95 латентности отказа; отсутствие утечки diff/ответа в логах | TRAINING_PR.diff:23-38 (app/api.py:35-38 — нет валидации), TRAINING_PR.diff:1-22 (app/review_service.py:19-22 — неограниченный prompt); adr.md:16-17 (422), adr.md:24 (413), adr.md:28-29 (логировать без diff), prompts.md:7-9 (P1-01/02/03 — большой diff в checks) |
| Таймаут/исключение LLM под нагрузкой (серия) | Параллельно 10–20 запросов; 2 серии по 20s; валидный небольшой diff. Подмена LLM.generate: sleep >10s и/или raise Exception. Инструмент: k6 с сценариями/подстановкой флага; метрика P95. | Таймаут LLM = 10s; контролируемая ошибка без 500. Код — Гипотеза: 504/503/429; зафиксировать фактический маппинг. p95 ≈ 10–11s (Гипотеза) при таймауте. | p50/p95 времени ответа; распределение кодов; отсутствие утечек в логах | adr.md:21-22 (timeout=10s, маппинг ошибок), analysis.md:31-34 (ошибка/timeout -> контролируемая ошибка), prompts.md:8-9 (P1-02/03 — исключение LLM в checks), TRAINING_PR.diff:1-22 (review_service.py:19-22 — нет обработки ошибок) |
| Пилообразные всплески (spikes) | 5 волн по 10 запросов, интервал 5s между волнами; валидный небольшой diff. Инструмент: k6 (stages) или vegeta burst; длительность ~30–60s; метрика P95/P99. | На валидном входе: ≤1% 5xx (цель из метрик), целимся в 0; 413/422 — только при намеренно неверном входе; 500 запрещён. p95 ≤ 2s (Гипотеза, зависит от LLM). | p95/p99 по волнам; процент ошибок; восстановление латентности после всплеска | problem.md:17-22 (цель — снизить долю 5xx), adr.md:28-29 (логировать длительность и статус), analysis.md:36-43 (валидный/граничный кейсы), tests_e2e.md:5-7 (ожидания по кодам 200/422/413) |
| Фоновый baseline (низкий RPS) | 0.5 RPS в течение 10 минут; валидный небольшой diff; эталон — заглушка LLM с быстрым ответом. Инструмент: vegeta/k6; метрика P95. | На валидном входе: ≤1% 5xx (цель), ожидаем 0%; p50 < 200ms, p95 < 400ms (Гипотеза со стабом LLM); фиксируем фактическое без стаба. | p50/p95 латентности; доля 5xx; стабильность кодов по сессии | problem.md:17-22 (снижение 5xx), adr.md:28-31 (OBS-1 логируем: request_id, длительность, статус), tests_integration.md:5-6; tests_unit.md:5-6 (ожидания по 422 и контролируемой ошибке LLM) |
| Массовая валидационная ошибка (422) | 20 RPS в течение 30s с телом {} или с отсутствующим/пустым diff. Инструмент: k6; метрика P95. | 100% ответов 422; без 500. p95 ≤ 300ms (Гипотеза) на отказ по валидации. | p95 латентности отказов; доля 422; отсутствие 5xx | analysis.md:38-43 (валидация входа и 422), adr.md:15-18 (Pydantic-модель и 422), tests_e2e.md:6 ({} => 422), prompts.md:7-9 (P1-01/02/03 — POST {} -> 500 AS IS, цель — 422) |

Как использовали AI:
- Строка в prompts.md: P2-RCTF
- Что проверили и исправили сами: (1) Явно зафиксировали политику кодов: 413/422 разрешены, 500 запрещён; (2) Добавили пятый сценарий «Массовая валидационная ошибка (422)»; (3) Для каждого сценария уточнили «Нагрузка и длительность», «Допустимый предел», «Что измеряем»; (4) Отметили численные значения как «Гипотеза» и добавили шаги замера (инструмент k6/vegeta, длительность, метрика p95); (5) Нормализовали Evidence с file:line и ссылками на adr.md/analysis.md/problem.md/prompts.md.

## Что изменили в исходном артефакте

- Файл и раздел:
- practices/practice_01/tests_load.md — целиком раздел «Нагрузочные проверки».
- Изменение:
  - Добавлена политика кодов ошибок (разрешены 413/422, запрещён 500) для всех сценариев.
  - Переформулированы и расширены сценарии до 5, каждому даны «Нагрузка и длительность», «Допустимый предел», «Что измеряем», «Evidence».
  - Добавлены пометки «Гипотеза» для численных порогов и конкретные шаги замера (инструмент k6/vegeta, длительность, метрика P95).
  - Нормализованы ссылки Evidence с форматами file:line и упоминанием adr.md/analysis.md/problem.md/prompts.md.
- Как проверили:
  - Согласовали с adr.md: строки 15-18 (валидация и 422), 21-22 (timeout=10s и маппинг ошибок), 24 (413), 28-31 (OBS-1 логирование).
  - Согласовали с analysis.md: 31-34 (контролируемая ошибка при timeout/исключении), 36-43 (валидный/граничный кейсы и 422/413).
  - Согласовали с problem.md: 17-22 (цель — снизить 5xx и явный контракт).
  - Согласовали с prompts.md: P1-01..P1-03 (evidence и checks: POST {} -> 500 AS IS; исключение LLM -> 500; большой diff -> деградация/ошибка).
- Что отклонили:
  - Фиксацию конкретного численного лимита размера diff — оставлено как процедура бинарного поиска (лимит зависит от реализации).
  - Жёсткую фиксацию кода для ошибок LLM (503/504/429) — отмечено как «Гипотеза», фактический маппинг зафиксируем после реализации error mapper.

# Few-shot

- Артефакт Практики 1: practices/practice_01/tests_load.md
- Что хотим улучшить: добавить проверяемые сценарии нагрузки, пределы (413/422), метрики, корректный evidence; очистить «Как использовали AI»

## Примеры

### Хороший результат

| Сценарий: Большой diff |
| Нагрузка и длительность: 1 файл = 25k символов, 1 запрос |
| Допустимый предел: HTTP 413 (или 422 при валидации), без 500 |
| Что измеряем: время ответа, код ошибки, логи без утечки содержимого |
| Evidence: app/api.py:35–38 (отсутствие явной валидации), adr.md (ограничение размера), prompts.md (P1-01/02/03) |

### Плохой результат

| Сценарий: «Сильно нагрузить сервер» |
| Нагрузка и длительность: «много запросов» |
| Допустимый предел: «чтобы не падало» |
| Что измеряем: «нагрузку» |
| Evidence: «гугл сказал» |

## Запрос

Ты — инженер по производительности. Цель: переписать и дополнить practices/practice_01/tests_load.md так, чтобы там было 3–5 воспроизводимых нагрузочных сценариев, четкие пределы, метрики и корректные ссылки на evidence из репозитория. Не придумывай факты — опирайся на материалы Практики 1: ADR, analysis.md, problem.md, prompts.md и TRAINING_PR.diff. Если точного числа нет — пометь как гипотезу и предложи способ замера.

Формат целевой таблицы для tests_load.md:
| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |

Хороший пример (следуй ему):
- Сценарий: Большой diff
- Нагрузка и длительность: 1 файл = 25k символов, 1 запрос
- Допустимый предел: HTTP 413 (или 422 при валидации), без 500
- Что измеряем: время ответа, код ошибки, логи без утечки содержимого
- Evidence: app/api.py:35–38 (отсутствие явной валидации), adr.md (ограничение размера), prompts.md (P1-01/02/03)

Плохой пример (не повторяй):
- Сценарий: «Сильно нагрузить сервер»
- Нагрузка и длительность: «много запросов»
- Допустимый предел: «чтобы не падало»
- Что измеряем: «нагрузку»
- Evidence: «гугл сказал» (нет ссылок на репозиторий)

Контекст и ограничения:
- По ADR: планируется валидация входа (422), ограничение размера diff (413) и таймаут LLM=10s с обработкой ошибок.
- В problem.md: цель — снизить долю 5xx, ввести явный контракт API.
- В prompts.md/analysis.md: подтверждены риски 500 на пустом diff, 500 при исключении LLM, деградация/ошибка на больших diff.
- Не добавляй внешние источники. Любые числа по RPS/latency — как гипотезы с явным способом замера.

Задача:
1) Предложи 4 сценария минимально достаточного набора:
   - Большой diff (граничный размер).
   - Таймаут/исключение LLM под нагрузкой (серия запросов).
   - Пилообразные всплески (spikes) при коротких сериях запросов.
   - Устойчивый фоновый трафик (низкий RPS) как baseline.
2) Для каждого — заполни таблицу строго в указанном формате.
3) Затем предложи короткий раздел «Как использовали AI» для tests_load.md:
   - Строка в prompts.md: P2-FS
   - Что проверили и исправили сами: конкретные правки (например, переписали таблицу; убрали дубликаты строк 12–15; уточнили пределы 413/422; добавили проверку на таймаут 10s)

## Что получили

Готовый фрагмент для practices/practice_01/tests_load.md:

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Большой diff (граничный размер) | 1 файл с diff длиной N символов, 1 запрос. N — гипотеза (подобрать бинарным поиском до срабатывания лимита). | 413 (или 422 при валидации), без 500 | Время ответа; код ошибки; логи без утечки содержимого | app/api.py:35-38 (невалидированный вход), app/review_service.py:19-22 (неограниченный prompt), practices/practice_01/adr.md:15-18,24 (422, 413), practices/practice_01/prompts.md:7-9 (P1-01/02/03 о большом diff) |
| Таймаут/исключение LLM под нагрузкой (серия) | Параллельно 10–20 запросов, 2 серии по 15–30s, валидный небольшой diff; подмена LLM.generate: sleep >10s и raise Exception | Таймаут LLM=10s; контролируемая ошибка без 500 (код — гипотеза: 504/503/429; зафиксировать выбранный) | p50/p95 времени ответа; доля ошибок по кодам; отсутствие утечки содержимого в логах | practices/practice_01/adr.md:21-22 (timeout=10s, маппинг ошибок), practices/practice_01/analysis.md:31-34 (ошибка/timeout), practices/practice_01/prompts.md:7-9 (P1-02/03 про исключение LLM), practices/practice_01/TRAINING_PR.diff:5-22 (нет обработки ошибок) |
| Пилообразные всплески (spikes) | 5 волн по 5–10 запросов с интервалом 3–5s между волнами; валидный небольшой diff | Без 5xx на валидном входе; 413/422 только при намеренно неверном входе | p95/p99 задержки по волнам; процент ошибок; стабилизация после всплеска | practices/practice_01/problem.md:17-22 (цель — снизить 5xx, явный контракт), practices/practice_01/adr.md:28-29 (логирование длительности и статусов), practices/practice_01/analysis.md:36-43 (валидный/граничный кейсы) |
| Фоновый baseline (низкий RPS) | 0.2–1 RPS в течение 5–10 минут; валидный небольшой diff; для эталона — заглушка LLM с быстрым ответом (гипотеза) | На валидном входе: 0% 5xx; p50 латентности — гипотеза (<200ms со стабом LLM); зафиксировать фактическое | p50/p95 латентности; доля 5xx; стабильность кодов | practices/practice_01/problem.md:17-22 (снижение 5xx), practices/practice_01/adr.md:28-31 (наблюдаемость), practices/practice_01/prompts.md:7-9 (checks базовых кейсов) |

Раздел «Как использовали AI»:
- Строка в prompts.md: P2-FS
- Что проверили и исправили сами: переписали таблицу сценариев под минимально достаточный набор (4 сценария); убрали дубликаты и двусмысленности; уточнили пределы 413/422 и добавили ссылку на timeout=10s; добавили метрики (p50/p95/p99, доля ошибок) и проверки отсутствия утечек; выровняли Evidence по adr.md/analysis.md/problem.md/prompts.md/TRAINING_PR.diff

## Что изменили в исходном артефакте

- Файл и раздел: practices/practice_01/tests_load.md (таблица и раздел «Как использовали AI»)
- Изменение: добавлены 4 сценария (большой diff, таймаут/исключение LLM под нагрузкой, spikes, baseline); уточнены пределы 413/422; добавлены метрики p50/p95/p99 и доля 5xx; добавлены корректные ссылки на evidence; удалены дубликаты и расплывчатые формулировки; упомянут таймаут 10s
- Как проверили: ссылки и формулировки согласованы с adr.md/analysis.md/problem.md/prompts.md и TRAINING_PR.diff; проверили, что числа без источника помечены как гипотезы с предложенным способом замера
- Что отклонили: всё без evidence
